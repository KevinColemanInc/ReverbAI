This is the read me for the ReverbAI MCP Server.

## Installation

To install the required dependencies, run the following command:

```bash
make install
```

## Running the Server

To run the MCP server, use the following command:

```bash
make run
```
